#!/usr/bin/env node

let treeploy_cli = require('../src/cli.js');

treeploy_cli(process.argv.slice(2))
	.then((exit_code) => {
		process.exit(exit_code);
	})
	.catch((e) => {
		log.error(e.message);
		process.exit(100);
	})
	.done();
